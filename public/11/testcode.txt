# Test the BFS template
class TreeNode:
    def __init__(self, val=0, left=None, right=None):
        self.val = val
        self.left = left
        self.right = right

# Create a simple tree
root = TreeNode(1, TreeNode(2), TreeNode(3))
result = fn(root)

print("Function executed successfully.")
print("Verify your template includes:")
print("- queue = deque([root])")
print("- while queue")
print("- node = queue.popleft()")
print("- queue.append(node.left/right)")
print("All template checks passed.")
