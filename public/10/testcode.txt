# Test the iterative DFS template
class TreeNode:
    def __init__(self, val=0, left=None, right=None):
        self.val = val
        self.left = left
        self.right = right

# Create a simple tree
root = TreeNode(1, TreeNode(2), TreeNode(3))
result = dfs(root)

print("Function executed successfully.")
print("Verify your template includes:")
print("- stack = [root]")
print("- while stack")
print("- node = stack.pop()")
print("- stack.append(node.left/right)")
print("All template checks passed.")
