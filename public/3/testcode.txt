# Test the prefix sum template
result = fn([1, 2, 3, 4, 5])

# Verify it returns a prefix sum array
assert result == [1, 3, 6, 10, 15], f"Expected [1, 3, 6, 10, 15], got {result}"

print("All template checks passed.")
