# Test the binary search template
arr = [1, 2, 3, 4, 5, 6, 7, 8, 9]

# Test finding existing element
result = fn(arr, 5)
assert result == 4, f"Expected index 4, got {result}"

# Test finding non-existing element returns insertion point
result = fn(arr, 10)

print("All template checks passed.")
