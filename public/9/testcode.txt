# Test the recursive DFS template
class TreeNode:
    def __init__(self, val=0, left=None, right=None):
        self.val = val
        self.left = left
        self.right = right

# Create a simple tree
root = TreeNode(1, TreeNode(2), TreeNode(3))
result = dfs(root)

print("Function executed successfully.")
print("Verify your template includes:")
print("- if not root: return")
print("- dfs(root.left)")
print("- dfs(root.right)")
print("All template checks passed.")
