# Binary Tree: DFS (Recursive)

Write the template for recursive depth-first search on a binary tree.

## When to Use
- Tree traversal problems
- Finding paths in trees
- Computing tree properties (height, sum, etc.)
- Most binary tree problems

## Template Structure
- Base case: check if root is None
- Process current node
- Recursively call on left and right children
- Return result
