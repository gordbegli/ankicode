# Graph: DFS (Recursive)

Write the template for recursive depth-first search on a graph.

## When to Use
- Graph traversal
- Finding connected components
- Detecting cycles
- Path finding in graphs

## Template Structure
- Use a seen set to track visited nodes
- Define recursive helper function
- Mark nodes as seen before recursing
- Iterate through neighbors
