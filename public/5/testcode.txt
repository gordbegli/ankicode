# Test the fast and slow pointer template
class ListNode:
    def __init__(self, val=0, next=None):
        self.val = val
        self.next = next

# Create a simple linked list: 1 -> 2 -> 3
head = ListNode(1, ListNode(2, ListNode(3)))
result = fn(head)

print("Function executed successfully.")
print("Verify your template includes:")
print("- slow = head, fast = head")
print("- while fast and fast.next")
print("- slow = slow.next")
print("- fast = fast.next.next")
print("All template checks passed.")
