# Test Dijkstra's algorithm
# distances should be defined after running the template

print("Verify your template includes:")
print("- distances = [inf] * n")
print("- distances[source] = 0")
print("- heap = [(0, source)]")
print("- while heap")
print("- curr_dist, node = heappop(heap)")
print("- if curr_dist > distances[node]: continue")
print("- for nei, weight in graph[node]")
print("- heappush(heap, (dist, nei))")
print("All template checks passed.")
