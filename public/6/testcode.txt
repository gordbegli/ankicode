# Test the reverse linked list template
class ListNode:
    def __init__(self, val=0, next=None):
        self.val = val
        self.next = next

def to_list(head):
    result = []
    while head:
        result.append(head.val)
        head = head.next
    return result

# Create: 1 -> 2 -> 3
head = ListNode(1, ListNode(2, ListNode(3)))
result = fn(head)

# Verify it returns reversed: 3 -> 2 -> 1
assert to_list(result) == [3, 2, 1], f"Expected [3, 2, 1], got {to_list(result)}"

print("All template checks passed.")
