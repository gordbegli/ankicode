# Find Top K Elements with Heap

Write the template for finding top k elements using a heap.

## When to Use
- Finding k largest/smallest elements
- Kth largest/smallest element
- Top k frequent elements
- Problems requiring partial sorting

## Template Structure
- Use min-heap of size k (for top k largest)
- Push elements with criteria
- Pop when heap exceeds size k
- Heap contains answer when done
