```
# Binary Tree: BFS

from collections import deque
def fn(root):
    stack = deque([root])
    while stack:
        lvlen = len(stack)
        for _ in range(lvlen):
            node = stack.popleft()
            if node.left:
                stack.append(node.left)
            if node.right:
                stack.append(node.right)
    return None



# Not quite.
# The code follows the BFS level-order loop structure, but it omits the per-level logic placeholder/comment block and does not include/maintain an answer variable (ans) as in the template.
# Press Cmd+Enter to try again, or Cmd+; to reveal.
```

```
# Subarrays with Exact Criteria

def fn(arr, k):
    counts = {}
    counts[0] = 1
    ans = 0
    for num in arr:
        #logic
        ans += counts[curr-k]
        counts[curr] += 1
    return ans
    


# Not quite.
# The core prefix-sum pattern is incomplete: `curr` is never initialized or updated, and `counts` is a plain dict without safe default behavior, so `counts[curr-k]`/`counts[curr] += 1` can error.
# Press Cmd+Enter to try again, or Cmd+; to reveal.
```